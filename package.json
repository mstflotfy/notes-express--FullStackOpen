{
  "name": "backend",
  "version": "0.0.1",
  "description": "first backend app",
  "main": "index.js",
  "scripts": {
    "start": "node index.js",
    "dev": "nodemon index.js",
    "test": "echo \"Error: no test specified\" && exit 1",
    "build:ui": "rm -rf dist && cd ../notes-react--FullStackOpen && npm run build && cp -r dist ../notes-express--FullStackOpen",
    "build:full": "npm run build:ui && git add . && git commit -m uibuild && git push"
  },
  "author": "mstflotfy",
  "license": "MIT",
  "dependencies": {
    "cors": "^2.8.5",
    "dotenv": "^16.4.5",
    "express": "^4.19.1",
    "mongoose": "^8.2.3"
  },
  "devDependencies": {
    "nodemon": "^3.1.0"
  }
}
